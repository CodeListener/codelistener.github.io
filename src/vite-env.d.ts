/// <reference types="vite/client" />

type Graduation = {
  hour: number;
  minute: number;
};

type ClockSetting = {
  SECONDS: number;
  MINUTES: number;
  HOURS: number;
  MONTH_ITEMS: number[];
  ONE_DAY_SECONDS: number;
  ONE_YEAR_SECONDS: number;
  DURATION: number;
};
